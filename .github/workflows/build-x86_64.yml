name: Build x86_64

on:
  workflow_dispatch:

env:
  TZ: Asia/Jakarta

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.owner.id }} == ${{ github.event.sender.id }}

    steps:
      - name: Checkout
        uses: actions/checkout@main
        
      - name: Initialization environment
        run: |
          sudo rm -rf /usr/share/dotnet /etc/apt/sources.list.d /usr/local/lib/android 2>/dev/null
          sudo -E apt-get -y update
          sudo -E apt-get -y install qemu-utils mkisofs zstd
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean

      - name: Building
        run: |
          chmod +x targets/x86_64.sh
          ./targets/x86_64.sh
          timedatectl set-timezone "${TZ}"
          echo "BUILD_TAG=VincherWrt-$(cat openwrt-version.txt)-$(date +"%Y.%m.%d")" >> $GITHUB_ENV
          echo "PACKAGED_STATUS=success" >> $GITHUB_ENV
 
      - name: Upload OpenWrt Firmware to Release
        uses: ncipollo/release-action@main
        if: ${{ env.PACKAGED_STATUS }} == 'success' && !cancelled()
        with:
          tag: ${{ env.BUILD_TAG }}
          artifacts: "openwrt/bin/targets/x86/64/*ext4-combined*,openwrt/bin/targets/x86/64/*squashfs-combined*"
          allowUpdates: true
          token: ${{ secrets.GITHUB_TOKEN }}
          bodyFile: release_body.txt